[project]
name = "my-workflows"
version = "0.1.0"
description = "daily life workflows"
requires-python = ">=3.10"
dependencies = [
  "prefect",
  "prefect-aws",
  "pyyaml",
  "pytest",
  "trafilatura",
]

[tool.uv]
package = false


[tool.pytest.ini_options]
pythonpath = ["."]
