[project]
name = "my-workflows"
version = "0.1.0"
description = "daily life workflows"
requires-python = ">=3.10"
dependencies = [
  "prefect",
  "prefect-aws",
  "pyyaml",
]

[tool.uv]
package = false
